spring:
  application:
    name: wifi-positioning-integration-service-test

server:
  port: 0



logging:
  level:
    com.wifi.positioning: DEBUG
    org.springframework: INFO

springdoc:
  api-docs:
    enabled: false
  swagger-ui:
    enabled: false 